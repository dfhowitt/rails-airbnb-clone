<div class="container">
  <% @bookings.each do |booking| %>
    <div>
      <h2><%= booking.property.name %></h2>
      <h3><%= booking.start_date %></h3>
      <h3><%= booking.end_date %></h3>
    </div>
    <hr>
  <% end %>
</div>
